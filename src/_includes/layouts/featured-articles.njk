<section id="featured-works" class="featured-works">
    <div class="container container--narrow flow">
        <h3 class="section-title">Featured <a href="blog">Blog</a> Posts <sub><i class="fa-solid fa-arrow-turn-down icon-accent"></i></sub></h3>
        <ul role="list" class="works__list">
            {%- for post in collections.post | reverse -%}
                {% if loop.index0 < 8 %}
                    {% include 'layouts/article-snippet.njk' %}
                {% endif %}
            {%- endfor -%}
        </ul>
    </div>
</section>