<section id="featured-works" class="featured-works blog-section">
    <div class="container container--narrow flow">
        <h3 class="section-title"><a href="#featured-works" class="anchor-tag">#</a> Featured <a href="works">Works</a></h3>
        <ul role="list" class="works__list">
            {%- for post in collections.featured_work | reverse -%}
                {% if loop.index0 < 8 %}
                    {% include 'layouts/works-snippet.njk' %}
                {% endif %}
            {%- endfor -%}
        </ul>
    </div>
</section>